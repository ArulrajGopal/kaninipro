# The main pipeline for databricks_SDP_SCD

resources:
  pipelines:
    databricks_SDP_SCD_etl:
      name: databricks_SDP_SCD_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/databricks_SDP_SCD_etl"

      libraries:
        - glob:
            include: ../src/databricks_SDP_SCD_etl/transformations/**