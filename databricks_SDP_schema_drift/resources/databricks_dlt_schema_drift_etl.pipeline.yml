# The main pipeline for databricks_dlt_schema_drift

resources:
  pipelines:
    databricks_dlt_schema_drift_etl:
      name: databricks_dlt_schema_drift_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/databricks_dlt_schema_drift_etl"

      libraries:
        - glob:
            include: ../src/databricks_dlt_schema_drift_etl/transformations/**

