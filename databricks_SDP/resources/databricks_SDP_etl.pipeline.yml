# The main pipeline for databricks_SDP

resources:
  pipelines:
    databricks_SDP_etl:
      name: databricks_SDP_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/databricks_SDP_etl"

      libraries:
        - glob:
            include: ../src/databricks_SDP_etl/transformations/**
